{
  "name": "think_view",
  "version": "1.1.2",
  "description": "View engine for ThinkKoa.",
  "main": "index.js",
  "scripts": {
    "test": "npm run eslint",
    "test-cov": "istanbul cover ./node_modules/mocha/bin/_mocha -- -t 50000 --recursive  -R spec test/",
    "compile": "babel src -d lib",
    "watch-compile": "npm run compile -- --watch",
    "prepublish": "npm run compile && npm test",
    "eslint": "eslint src"
  },
  "engines": {
    "node": ">6.0.0"
  },
  "author": "richenlin",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.24.1",
    "babel-eslint": "^7.2.3",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.5.1",
    "eslint": "^3.19.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.4.2"
  },
  "dependencies": {
    "ejs": "^2.5.6",
    "think_lib": "^1.x.x"
  }
}
